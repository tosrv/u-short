app = 'u-short'
primary_region = 'sin'

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[mounts]
  source = 'u_short_data'
  destination = '/data'
  processes = ['app']

[env]
  BASE_URL = 'https://u-short.fly.dev/'

[[vm]]
  size = "shared-cpu-1x"
  memory_mb = 256

